# Table App (Dockerized)

This sample project contains a minimal Flask app that accepts a number from the user and displays its multiplication table (1–10). The app is dockerized using the included `Dockerfile`.

Quick start:

- Build: `docker build -t table_app .`
- Run: `docker run -p 5000:5000 table_app`

